import { NextResponse } from "next/server";

// Phase 0 placeholder — real handler built in Phase 3 (Sign-in)
export async function POST() {
  return NextResponse.json({ message: "Endpoint not yet implemented" }, { status: 501 });
}
