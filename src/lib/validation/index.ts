// Phase 0 placeholder — single shared Zod schema module implemented in Phase 1.
// All validation rules are declared once here, imported by both API routes and client forms (AGENTS.md 2.4).
export {};
