// Phase 0 placeholder — Argon2id password hashing implemented in Phase 1 (Signup).
// Parameter constraints locked in AGENTS.md 2.3: memory 19 MiB (19456 KiB), 2 iterations, parallelism 1.
// Heavy work must run in Node.js runtime API routes ONLY, never in middleware (AGENTS.md 4.1).
export {};
